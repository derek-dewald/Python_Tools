import pandas as pd
import numpy as np
import datetime

import sys
sys.path.append("/Users/derekdewald/Documents/Python/Github_Repo/d_py_functions")

from objects_automated import object_dict
consolidated_df = pd.read_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx')
knowledge_base_df = pd.read_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx')
link_df = pd.read_csv(object_dict['csv_links']['python_object']['d_links'])
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
technical_notes = pd.read_csv(object_dict['csv_links']['python_object']['technical_notes'])
definition_df.head(1)
knowledge_base_df.head()
def missing_processes_from_knowledgebase(knowledge_base_df,definition_df):

    temp_df = definition_df[['Process','Categorization','Word',"Definition"]].copy()

    kb_temp = knowledge_base_df[['Process']].drop_duplicates().reset_index(drop=True)
    kb_temp['PROC_IN_KNOWLEDGE_BASE']= 1

    kb_temp1 = knowledge_base_df[['Word']].drop_duplicates().reset_index(drop=True)
    kb_temp1['WORD_IN_KNOWLEDGE_BASE']= 1


    temp_df = temp_df.merge(kb_temp,on='Process',how='left').merge(kb_temp1,on='Word',how='left').fillna(0)

    return temp_df

missing_processes_from_knowledgebase(knowledge_base_df,definition_df)
def missing_processes_from_knowledgebase(knowledge_base_df,definition_df):

    temp_df = definition_df[['Process','Categorization','Word',"Definition"]].copy()

    kb_temp = knowledge_base_df[['Process']].drop_duplicates().reset_index(drop=True)
    kb_temp['PROC_IN_KNOWLEDGE_BASE']= 1

    kb_temp1 = knowledge_base_df[['Word']].drop_duplicates().reset_index(drop=True)
    kb_temp1['WORD_IN_KNOWLEDGE_BASE']= 1


    temp_df = temp_df.merge(kb_temp,on='Process',how='left').merge(kb_temp1,on='Word',how='left').fillna(0)

    return temp_df

d = missing_processes_from_knowledgebase(knowledge_base_df,definition_df)

d[d['PROC_IN_KNOWLEDGE_BASE']==0]
def missing_processes_from_knowledgebase(knowledge_base_df,definition_df):

    temp_df = definition_df[['Process','Categorization','Word',"Definition"]].copy()

    kb_temp = knowledge_base_df[['Process']].drop_duplicates().reset_index(drop=True)
    kb_temp['PROC_IN_KNOWLEDGE_BASE']= 1

    kb_temp1 = knowledge_base_df[['Word']].drop_duplicates().reset_index(drop=True)
    kb_temp1['WORD_IN_KNOWLEDGE_BASE']= 1


    temp_df = temp_df.merge(kb_temp,on='Process',how='left').merge(kb_temp1,on='Word',how='left').fillna(0)

    return temp_df

d = missing_processes_from_knowledgebase(knowledge_base_df,definition_df)

d[d['PROC_IN_KNOWLEDGE_BASE']==0]['Process'].value_counts()
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
display(notes_df)
display(definition_df)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
display(notes_df)
display(definition_df)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv']).fillna('')
display(notes_df)
display(definition_df)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')
display(notes_df)
display(definition_df)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')


# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Merge in 

display(notes_df)

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word"}),on='Word',how='left')
notes_df1

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':"Definition_'}),on='Word',how='left')
notes_df1

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True).fillna("")
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.fillna("").drop('Definition_',axis=1,inplace=True)
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'].fillna("")
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left')
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'].fillna(""))
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna(")
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
pd.concat([definition_df,notes_df1])
pd.concat([definition_df,notes_df1]).reset_index(drop=True)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
pd.concat([definition_df,notes_df1]).reset_index(drop=True)
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
pd.concat([definition_df,notes_df1]).reset_index(drop=True)
knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
knowledge_base_df
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
processes
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process']
processes
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process']
processes
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process']
processes
#proceses.merge(
not_processes
not_processes
not_processes.tail(20)
not_processes[not_processes['Process'].isin(processes['Process'].unique()]
not_processes[not_processes['Process'].isin(processes['Process'].unique())]
pd.concat([processes,
           not_processes[not_processes['Process'].isin(processes['Process'].unique())]
          ])
pd.concat([processes,
           not_processes[not_processes['Process'].isin(processes['Process'].unique())]
          ]).sort_values('Process')
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index()
processes
not_processes
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})
processes
not_processes
pd.concat([processes,
           not_processes[not_processes['Process'].isin(processes['Process'].unique())]
          ]).sort_values('Process')
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
]).sort_values('Process')

final_df
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
])

cat_order_dict = ['Process':0,'Process Step':1])

final_df['CAT_ORDER'] = final_df['Categorization'].map(cat_order_dict)

final_df#.sort_values(
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
])

cat_order_dict = ['Process':0,'Process Step':1})

final_df['CAT_ORDER'] = final_df['Categorization'].map(cat_order_dict)

final_df#.sort_values(
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
])

cat_order_dict = {'Process':0,'Process Step':1}

final_df['CAT_ORDER'] = final_df['Categorization'].map(cat_order_dict)

final_df#.sort_values(
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
])

cat_order_dict = {'Process':0,'Process Step':1}

final_df['CAT_ORDER'] = final_df['Categorization'].map(cat_order_dict)

final_df.sort_values(['Process','CAT_ORDER','NP_ORDER'])
final_df = pd.concat([
    processes,
    not_processes[not_processes['Process'].isin(processes['Process'].unique())]
])

cat_order_dict = {'Process':0,'Process Step':1}
final_df['CAT_ORDER'] = final_df['Categorization'].map(cat_order_dict)
final_df = final_df.sort_values(['Process','CAT_ORDER','NP_ORDER'])
final_df
not_processes[not_processes['Process'].isin(processes['Process'].unique())]
np_df = not_processes[not_processes['Process'].isin(processes['Process'].unique())]
np_df.head(1)
np_df
np_df[np_df['Process']=='Goal Setting']
final_df[final_df['Word']=='Goal Setting']
np_df.merge(final_df[['Word']].unique().rename(columns={'Word':"Process"}),on='Word',how='inner')
np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Word',how='inner')
final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"})
np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental = np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1)
supplemental = np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)
supplemental = np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)

final_df1 = pd.concat[final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)

final_df1 = pd.concat(
    [final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)

final_df1 = pd.concat([final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)

final_df1 = pd.concat([final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental.drop('Process',axis=1,inplace=True)

final_df1 = pd.concat([final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])
final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
#final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])


#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
supplemental = np_df.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])

final_df1
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])


#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

# Start with Items where Categorization is Process. These are Processes that Exist.

# Process refers to the Record itself.
# Categorization refers to the Type of Record that the individual item is. Which can Include Process, as it defines the charactersitics of the record

#display(definition_df)

# Before Merging Notes in see if there are Definitions which can be applied.

#display(notes_df)

notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
notes_df1.drop('Definition_',axis=1,inplace=True)
notes_df1
knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
knowledge_base_df
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process']
not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})
processes.head()
not_processes.head()
not_processes
np_df
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
supplemental
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])

final_df1
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])


#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])

final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])


#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.sort_values(['Process','CAT_ORDER','NP_ORDER'])

final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1
processes
# Need to Order.
processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})
processes.head()
processes
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]],on='Process',how='left')
final_df1

final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])

final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]],on='Process',how='left')
final_df1

#final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
processes
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
final_df1

#final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')

final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
#final_df1.drop('Word_',inplace=True,axis=1)
#final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')

final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
#final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
#final_df1.drop('Word_',inplace=True,axis=1)
final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')

final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
final_df1.drop('Word_',inplace=True,axis=1)
final_df1
supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)

# Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
final_df1 = pd.concat([final_df,supplemental])
final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')

final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])

final_df1.drop('Word_',inplace=True,axis=1)
final_df1
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame()
):
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop('Word_',inplace=True,axis=1)
    
    return final_df1

    
cd = generate_files_for_streamlit(definition_df,notes_df)
cd
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame()
):
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop('Word_',inplace=True,axis=1)
    
    return knowledge_base_df,processes,final_df1

    
knowledge_base_df,processes,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
processes
consolidated_df
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame()
):
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop('Word_',inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()
    
    return knowledge_base_df,process_df,final_df1

knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop('Word_',inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1

knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df.head(1)
process_df.head(1)
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1

knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df.head(1)
process_df.head(1)
consolidated_df.head(1)
consolidated_df
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])[['Process','Categorization','Word',"Definition"]].fillna('')

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df.head(1)
process_df.head(1)
consolidated_df.head(1)
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1
knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df.head(1)
process_df.head(1)
consolidated_df.head(1)
consolidated_df
process_df
knowledge_base_df
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[definition_df['Word']=='Definition'][['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1
knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df
knowledge_base_df
knowledge_base_df
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df
process_df
consolidated_df
consolidated_df
knowledge_base_df.head(30)
knowledge_base_df.iloc[:30]
knowledge_base_df.iloc[30:]
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[definition_df['Word']=='Definition'][['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    return processes,not_processes

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(final_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    final_df1 = pd.concat([final_df,supplemental])
    final_df1 = final_df1.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    final_df1 = final_df1.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    final_df1['Categorization'] = np.where(final_df1['Word_'].notnull(),final_df1['Word'],final_df1['Categorization'])
    final_df1['Word'] = np.where(final_df1['Word_'].notnull(),final_df1['Word_'],final_df1['Word'])
    
    final_df1.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = final_df1[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        final_df1.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,final_df1
#knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
processes,not_processes = generate_files_for_streamlit(definition_df,notes_df)
processes
not_processes
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[definition_df['Word']=='Definition'][['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    return knowledge_base_df,processes,not_processes

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(knowledge_base_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    consolidated_data = pd.concat([knowledge_base_df,supplemental])
    consolidated_data = consolidated_data.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    consolidated_data = consolidated_data.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    consolidated_data['Categorization'] = np.where(consolidated_data['Word_'].notnull(),final_df1['Word'],consolidated_data['Categorization'])
    consolidated_data['Word'] = np.where(consolidated_data['Word_'].notnull(),consolidated_data['Word_'],consolidated_data['Word'])
    
    consolidated_data.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = consolidated_data[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        consolidated_data.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,consolidated_data
#knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
knowledge_base_df,processes,not_processes = generate_files_for_streamlit(definition_df,notes_df)
supplemental = not_processes.merge(knowledge_base_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
supplemental['Word_'] = supplemental['Word'].copy()
supplemental['Word'] = supplemental['Process'].copy()
supplemental['Process'] = supplemental['Process_'].copy()
supplemental.drop('Process_',axis=1,inplace=True)
supplemental
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[definition_df['Word']=='Definition'][['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    #return knowledge_base_df,processes,not_processes

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(knowledge_base_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    consolidated_data = pd.concat([knowledge_base_df,supplemental])
    consolidated_data = consolidated_data.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    consolidated_data = consolidated_data.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    consolidated_data['Categorization'] = np.where(consolidated_data['Word_'].notnull(),final_df1['Word'],consolidated_data['Categorization'])
    consolidated_data['Word'] = np.where(consolidated_data['Word_'].notnull(),consolidated_data['Word_'],consolidated_data['Word'])
    
    consolidated_data.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = consolidated_data[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        consolidated_data.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,consolidated_data
notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv'])
definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])
def generate_files_for_streamlit(
    definition_df=pd.DataFrame(),
    notes_df=pd.DataFrame(),
    generate_excel_files=True
):

    '''
    Definition:
        Process Utilized to Combine Notes/ Definitions and Logic into Knowledge Base, which is utilized to Create, Processes, Parameters.
    Parameters:
        notes_df (dataframe): Dataframe containing Notes from Google. Default is none and it will pull directly from Google.
        definition_df (dataframe): Dataframe containing Definitions from Google. Default is none and it will pull directly from Google.
        manual_object_df (dataframe): Dataframe containining Dataframe of Parameters, generated from Python Process _____, which converts lists in objects_manual.py.
        export_location(str): Name of File to export excel file to. If Blank, returns nothing
    Returns:
        Excel File
    Date Created:
        02-Jul-26
    Date Last Modified:
        22-Jul-26
    Process:
        Definition
    Categorization:
        Definition
    Usage:
        d = generate_knowledgebase(notes_df,definition_df,manual_object_df)
        d = generate_knowledgebase()
    Notes:
        22-Jul - Overhauled merge. Attempted to streamline, simplify and reduce duplication. Increase Visability.
        28-Jul - Originally Named - generate_knowledge_base, stream lined to remove usage of Dictionary, which were complex and operationally inefficient.
    '''
    
    if len(definition_df)==0:
        definition_df = pd.read_csv(object_dict['csv_links']['python_object']['google_definition_csv'])

    if len(notes_df)==0:
        notes_df = pd.read_csv(object_dict['csv_links']['python_object']['google_notes_csv']).fillna('')

    # Before Merging Files. Update Notes to include Definitions from any item which is a Process from Definition.

    definition_df = definition_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()
    notes_df = notes_df[['Process','Categorization','Word',"Definition"]].fillna('').copy()

    notes_df1 = notes_df.merge(definition_df[definition_df['Word']=='Definition'][['Process','Definition']].rename(columns={'Process':"Word",'Definition':'Definition_'}),on='Word',how='left').fillna("")
    notes_df1['Definition'] = np.where((notes_df1['Categorization']=='Process Step')&(notes_df1['Definition']==""),notes_df1['Definition_'],notes_df1['Definition'])
    notes_df1.drop('Definition_',axis=1,inplace=True)

    # CREATE KNOWLEDGE File, which is simply everything combined Together
    knowledge_base_df = pd.concat([definition_df,notes_df1]).reset_index(drop=True)
    
    # Create 2 Distinct Files Processes. Not Processes
    processes = knowledge_base_df[knowledge_base_df['Categorization']=='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'PROC_ORDER'})
    not_processes = knowledge_base_df[knowledge_base_df['Categorization']!='Process'].reset_index(drop=True).reset_index().rename(columns={'index':'NP_ORDER'})

    #return knowledge_base_df,processes,not_processes

    # Create a Supplemental File which are effectively Items which need to be merged into Processes to move from the Straw Man Process to a more fullsome 
    supplemental = not_processes.merge(knowledge_base_df[['Word','Process']].drop_duplicates().rename(columns={'Process':'Process_','Word':"Process"}),on='Process',how='inner')
    supplemental['Word_'] = supplemental['Word'].copy()
    supplemental['Word'] = supplemental['Process'].copy()
    supplemental['Process'] = supplemental['Process_'].copy()
    supplemental.drop('Process_',axis=1,inplace=True)

    # Before Supplemental can be finalized, needs to merge into Final DF. to ORder BEFORE replacing Word with Word_
    consolidated_data = pd.concat([knowledge_base_df,supplemental])
    consolidated_data = consolidated_data.merge(processes[['PROC_ORDER',"Process"]].rename(columns={'Process':"Word"}),on='Word',how='left')
    
    consolidated_data = consolidated_data.sort_values(['Process','PROC_ORDER','CAT_ORDER','NP_ORDER'])
    consolidated_data['Categorization'] = np.where(consolidated_data['Word_'].notnull(),final_df1['Word'],consolidated_data['Categorization'])
    consolidated_data['Word'] = np.where(consolidated_data['Word_'].notnull(),consolidated_data['Word_'],consolidated_data['Word'])
    
    consolidated_data.drop(['Word_','PROC_ORDER','CAT_ORDER','NP_ORDER'],inplace=True,axis=1)

    # Create a DataFrame for the Highlevel Process, which is Processes and Process Steps.
    process_df = consolidated_data[final_df1['Categorization'].isin(['Process','Process Step'])].copy()

    if generate_excel_files:
        knowledge_base_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/knowledge_base.xlsx',index=False)
        process_df.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/defined_processes.xlsx',index=False)
        consolidated_data.to_excel('/Users/derekdewald/Documents/Python/Github_Repo/Streamlit/Data/consolidated_dataset.xlsx',index=False)
    
    return knowledge_base_df,process_df,consolidated_data
knowledge_base_df,process_df,consolidated_df = generate_files_for_streamlit(definition_df,notes_df)
#knowledge_base_df,processes,not_processes = generate_files_for_streamlit(definition_df,notes_df)
%history -f text.txt
